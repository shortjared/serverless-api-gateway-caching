const APP_ROOT = '..';
const given = require(`${APP_ROOT}/test/steps/given`);
const ApiGatewayCachingSettings = require(`${APP_ROOT}/src/ApiGatewayCachingSettings`);
const updatePathParametersCacheSettings = require(`${APP_ROOT}/src/newPathParametersCache`);
const expect = require('chai').expect;

describe('Updating path parameter cache settings', () => {

});
